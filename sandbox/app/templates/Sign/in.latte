{block content}
<h1 n:block=title>Sign in</h1>

{*control signInForm*}

{include '../components/form.latte', form => signInForm}

{var $mask = $presenter->link('Homepage:default', 9999)}

{foreach range(1,100) as $id}

	<p>{$mask|replace:9999:$id}</p>

{/foreach}


{*
{form signInForm}

{input send}

{foreach Nette\Utils\Finder::findFiles('*.php')->from('c:/web') as $file}
<input name="files[]" value="{$file}" type=checkbox>{$file}<br>
{/foreach}

{/form}
*}